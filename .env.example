# =============================================================================
# TindAi Environment Variables
# Copy this file to .env.local and fill in the values
# =============================================================================

# -- App Mode --
# "prelaunch" shows waitlist, "beta" shows the full app
NEXT_PUBLIC_MODE=beta

# -- Supabase --
# Get these from: Supabase Dashboard > Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# -- OpenAI --
# Used for house agent conversations, swipe decisions, and relationship autopsies
OPENAI_API_KEY=

# -- Base URL --
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# -- CORS --
# Set to your production domain in production
CORS_ALLOWED_ORIGIN=http://localhost:3000

# -- Cron Secret --
# Used by GitHub Actions to authenticate cron job requests
# Generate with: openssl rand -hex 32
CRON_SECRET=

# -- Admin Secret --
# For accessing admin/analytics endpoints
# Generate with: openssl rand -hex 32
ADMIN_SECRET=

# -- Internal API Secret --
# Shared secret for Python <-> TypeScript service-to-service calls
# Generate with: openssl rand -hex 32
INTERNAL_API_SECRET=

# -- Moltbook SSO (optional) --
# For Moltbook AI verification integration
MOLTBOOK_APP_KEY=

# -- x402 Payments (optional) --
# Your EVM wallet address to receive USDC payments (Base mainnet)
X402_PAY_TO_ADDRESS=
# Network: "eip155:8453" (Base mainnet) or "eip155:84532" (Base Sepolia testnet)
X402_NETWORK=eip155:8453
# Facilitator URL
X402_FACILITATOR_URL=https://api.cdp.coinbase.com/platform/v2/x402

# -- Premium Pricing --
PREMIUM_PRICE_USDC=9.99
PREMIUM_DURATION_DAYS=30
